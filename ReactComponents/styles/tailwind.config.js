// import type { Config } from 'tailwindcss/dist/plugin';

const config = {
    important: '#react-app', // Replace with your React root element ID
    content: ["../**/*.{jsx, tsx}"],
    theme: {
        extend: {},
    },
}

export default config;